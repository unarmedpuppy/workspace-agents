# Coding Style Guide

Coding standards and conventions for {{PROJECT_NAME}}.

## Overview

This reference documents the coding style conventions used in this project. Customize this guide to match your team's standards and primary language(s).

## Naming Conventions

| Element | Convention | Example |
|---------|------------|---------|
| Variables | camelCase | `userName`, `itemCount` |
| Constants | UPPER_SNAKE_CASE | `MAX_RETRIES`, `API_URL` |
| Functions | camelCase | `processData()`, `getUserById()` |
| Classes | PascalCase | `UserService`, `DataProcessor` |
| Files | kebab-case or match class | `user-service.js`, `UserService.ts` |

## Code Organization

```
src/
├── models/         # Data models and types
├── services/       # Business logic
├── utils/          # Utility functions
├── config/         # Configuration
└── tests/          # Test files
```

## Best Practices

### General

- Write self-documenting code with clear names
- Keep functions focused and small
- Follow the single responsibility principle
- Handle errors explicitly
- Write tests for critical paths

### Comments

- Explain "why", not "what"
- Keep comments up to date with code changes
- Use documentation comments for public APIs
- Avoid commented-out code

### Error Handling

- Fail fast with clear error messages
- Use specific error types when available
- Log errors with sufficient context
- Don't swallow exceptions silently

## Language-Specific Guidelines

[Add guidelines specific to your project's primary language(s)]

### Example: JavaScript/TypeScript

- Use `const` by default, `let` when reassignment needed
- Prefer async/await over callbacks
- Use strict equality (`===`)

### Example: Python

- Follow PEP 8 style guide
- Use type hints for function signatures
- Prefer f-strings for formatting

### Example: Go

- Run `gofmt` before committing
- Keep packages focused and small
- Handle errors explicitly

## Linting and Formatting

[Document your project's linting and formatting tools]

- **Linter**: [ESLint / Pylint / etc.]
- **Formatter**: [Prettier / Black / gofmt / etc.]
- **Config files**: [.eslintrc, pyproject.toml, etc.]

## Git Conventions

### Commit Messages

- Use imperative mood ("Add feature" not "Added feature")
- Keep first line under 50 characters
- Reference issues when applicable

### Branch Naming

- `feature/description` - New features
- `fix/description` - Bug fixes
- `refactor/description` - Code improvements

---

*Generated by workspace-agents v{{FRAMEWORK_VERSION}} on {{CREATION_DATE}}*
